name: UPH App Tests

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  test:
    uses: frappe/frappe/.github/workflows/_base-server-tests.yml@develop
    with:
      app_name: "uph"
      frappe_branch: "version-15"
      db: "mariadb"
      type: "server"
      python_version: "3.12"
